import 'package:flutter/cupertino.dart';

// TODO: More maze type, empty widget for placeholder
class HexagonalMaze extends StatelessWidget {
  const HexagonalMaze({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
